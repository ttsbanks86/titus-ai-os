---
description: Mass video production pipeline: animations, 3D, Remotion, HyperFrames. Extends hyperframes, hyperframes-cli, hyperframes-media, remotion-to-hyperframes. Heavy compute only with approval.
---

Use the `video-pipeline` skill.

Classify request: new composition, translate Remotion, batch render, install registry item. Use existing HyperFrames skills. Produce composition, render queue, export manifest. Heavy GPU only with approval.

User request: $ARGUMENTS
