---
description: Run or explain the Titus Personal AI Operator desktop assistant for local model routing, desktop control, daily briefings, browser launch, business ops, and Graphify handoff.
---

Use the `personal-ai-operator` skill.

If the user asks to run a safe local command, use:

```powershell
powershell.exe -ExecutionPolicy Bypass -File "C:\Users\tbank\Desktop\Live Cowork\PERSONAL-AI-OPERATOR\Start-PersonalAIOperator.ps1" -Command help
```

Available operator commands: `help`, `ask`, `brief`, `apps`, `screenshot`, `open-url`, `graphify`, `business`, `safety`.

User request: $ARGUMENTS
